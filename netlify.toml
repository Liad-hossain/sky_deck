[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Bundle the runtime secrets file alongside the function code.
[functions]
  included_files = ["netlify/functions/secrets/secrets.json"]

# ── API: rewrite /api/* → single Hono router function ────────
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/urls/:splat"
  status = 200

# ── SPA fallback ──────────────────────────────────────────────
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
